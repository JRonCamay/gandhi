CRITICAL ADDITION: The Manager guardian check MUST be the first executable statement inside every managed function or loop. No reads, writes, rendering, VM access, DOM updates, calculations, side effects, logging, or event handling may occur before the guardian. Pattern: function update(){ if (manager.currStation !== MY_STATION_ID) return; ... }. This guarantees that execution order is enforced before any work begins.